{
  "task_id": "task-genesis-funding-quickstart",
  "task_description": "Deliver 4 ordered items on the matrix-os monorepo: (1) update root README screenshots section to the new multi-page IA with accurate native-first captions (images already captured/committed by orchestrator); (2) wire the existing Treasury genesis/reward-pool logic into the live node + config and expose a funding path (market gRPC FundAccount + matrix fund CLI) so a buyer can obtain native MATRIX, then demonstrate the full fund->job->complete->settled loop on a live node with a real transcript; (3) a one-command copy-paste quickstart via the matrix CLI plus click-to-copy command snippets on the website (home + /products/cli); (4) remaining backlog in order (wire inference service + matrix inference CLI end to end with GPU-free echo backend; bridge burn->unlock watcher if achievable else honest runnable piece; minimal web tests if reasonable) then an honest STILL-REMAINING list. Never edit .github/workflows/* into main-bound commits.",
  "status": "completed",
  "feature_order": ["FEAT-001", "FEAT-002", "FEAT-003", "FEAT-004"],
  "blocked_reason": null,
  "verification": {
    "build": "pass",
    "tests": "pass",
    "test_quality": "pass",
    "docker_build": "skipped",
    "summary": "make proto OK; services/core go build/vet/test ./... all pass; go test -race on internal/{node,marketapi,cli,token,inference,inferenceapi,bridge,p2p} all pass; contracts npx hardhat test 38 passing; apps/web corepack yarn install --frozen-lockfile + lint + build all clean. No Dockerfile for the touched services so docker_build skipped. Test-quality spot check: genesis_test.go opens a real Pebble store, applies genesis, closes+reopens to simulate restart, and asserts idempotency (would fail if double-crediting); marketapi FundAccount handler is real (calls Funder.FundFromRewardPool, maps over-pool to FailedPrecondition, Unimplemented without funder) and covered by fund_test.go; bridge watcher proven live against a local hardhat node. Live transcripts captured for the fund->job->settle loop, matrix quickstart, matrix inference (echo), and bridge burn->unlock."
  },
  "review": {
    "v1": "NEEDS_CHANGES (2026-09-06-124319-review.md): 5 findings - dead watcher cursor constant/false comments, overstated bridge-watch matrixd integration comment, FundAccount unauthenticated drain on ACL-disabled nodes, inference web one-liner missing prerequisites, wallet loader missing key-pairing check.",
    "fix_commit": "86c7d39 - all 5 findings fixed (FundAccount now requires auth-enforced server, wallet loaders derive-and-compare, watcher/bridge-watch comments corrected, inference web block now fund->register->submit).",
    "v2": "APPROVED (2026-09-06-125913-review.md): all 5 findings resolved, 0 open issues, no regressions; fund-auth gate backed by regression tests, legitimate funding path preserved."
  }
}
